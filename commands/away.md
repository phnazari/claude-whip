---
description: Away mode — push a native notification to my phone (Claude app) whenever something new lands
---

Away mode is now ON. Until the user runs `/back`:

1. Whenever something new lands — a run finishes, a result arrives, something fails,
   or you are blocked on the user — call the PushNotification tool with a one-line
   outcome, in addition to responding as you normally would. Do not push for ordinary
   conversational turns. Does not expire and does not lapse on topic change.
2. If Remote Control is not connected, tell the user to run `/remote-control` NOW (before
   they leave) so pushes reach the Claude app on their phone — you cannot enable it yourself.

Confirm in one line that away mode is armed.
